window.HELP_IMPROVE_VIDEOJS = false;


$(document).ready(function() {
    // Check for click events on the navbar burger icon

    if (typeof window.initPlannerDemo === 'function') {
      window.initPlannerDemo();
    }

	})
